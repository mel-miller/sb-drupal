import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as {{ properCase foundationName }}Stories from './{{ dashCase foundationName }}.stories';

<Meta of={ {{ properCase foundationName }}Stories } />

# {{ properCase foundationName }}

Custom documentation for the {{ dashCase foundationName }} component.

<Canvas of={ {{ properCase foundationName }}Stories.Default } />
