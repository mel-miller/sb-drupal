import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as {{ properCase utilityName }}Stories from './{{ dashCase utilityName }}.stories';

<Meta of={ {{ properCase utilityName }}Stories } />

# {{ properCase utilityName }}

Custom documentation for the {{ dashCase utilityName }} component.

<Canvas of={ {{ properCase utilityName }}Stories.Default } />
